// Temporary re-export so the not-yet-converted `.mjs` importers keep resolving during the TypeScript
// conversion. Delete once every importer names `./strip-code.ts`.
export * from './strip-code.ts';
