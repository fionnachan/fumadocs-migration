# Code owners for the Arbitrum docs portal.
#
# No `@OffchainLabs/docs` (or similarly named) GitHub team exists for this repo — checked via
# `gh api orgs/OffchainLabs/teams --paginate`, which lists org teams including "Technical Writing"
# and "Full Stack" but nothing scoped to this repo's docs work. Rather than guess at a team that
# doesn't exist, owners below are the individual GitHub handles who actually authored the recent
# history of this repo, confirmed via `git log --format=%an` and
# `gh pr list --state merged --limit 30 --json author`. Replace these with a real team once one is
# created for this repo.

# Content, components, scripts, and CI/workflow config.
/content/    @anegg0 @pete-vielhaber
/components/ @anegg0 @pete-vielhaber
/scripts/    @anegg0 @pete-vielhaber
/.github/    @anegg0 @pete-vielhaber
